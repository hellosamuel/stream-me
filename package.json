{
  "name": "stream-me",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "scripts": {
    "dev:api": "yarn workspace @stream-me/api dev",
    "dev:app": "yarn workspace @stream-me/app dev",
    "dev": "lerna run dev --scope={@stream-me/api,@stream-me/app}"
  },
  "prettier": {
    "semi": false,
    "printWidth": 120,
    "arrowParens": "avoid"
  }
}
